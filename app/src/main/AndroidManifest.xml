<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abhijai.arp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.ui.timeline.TimeLineKotlin"></activity>
        <activity android:name=".views.ui.timeline.TimeLineActivity" />
        <activity
            android:name=".views.NavFeaturesActivity"
            android:label="@string/title_activity_nav_features"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.BaseActivity" />
        <activity android:name=".views.ui.jobcards.JobCardCandidateActivity" />
        <activity android:name=".views.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>